#Installation:

    git clone git://github.com/metti/dotvim.git ~/.vim

#Create symlink:

    ln -s ~/.vim/vimrc ~/.vimrc

#Fetch submodules:

    pushd ~/.vim && git submodule init && git submodule update && popd
